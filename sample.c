#include <stdio.h>
//#include <string.h>

int main()
{
       //char src[40];

          int n;
          scanf("%d",&n);
            
          char dest[n];
             //memset(dest, '\0', sizeof(dest));
                strcpy(src, "This is tutorialspoint.com");
                   strcpy(dest, src);char strx[44];strcpy(strx, src);

                      printf("Final copied string : %s\n", dest);
                         
                         return(0);
}
